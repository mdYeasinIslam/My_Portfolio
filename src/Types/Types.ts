export type ProjectType = {
    id:number
    title: string,
    description: string
    image: string,
    features: string[]
    technology: string[]
     challenges: string[],
    future_plan:string[],
    github:string
    liveSite:string
}